#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char ** argv)
{
	char line[100]; 
	char *p;
	s = argv[i]
	FILE *fp = (argv[i], "r");
	for(int i = 1; i < argc, i++){
		while(fgets(line, 100, fp) != NULL){
			p = strtok(" ");
			while(p != NULL){
				printf("%s\n", p);
			}
		}
	}
}
